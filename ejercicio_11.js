let lista = [1, 0.1, "Hola", false]
for (let i = 0; i < lista.length; i++){
    console.log(lista[i], typeof lista[i])
}

function iterarArray(arr){
    for (let i = 0; i < arr.length; i++){
        console.log(arr[i], typeof arr[i])
    }
}